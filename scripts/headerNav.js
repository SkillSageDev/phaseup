const items = document.querySelectorAll("header > nav > ul > li");

// const navURLs = ["./../index.html", "./../index.html", ""];

// items.forEach(item => {
//     item.addEventListener("click", () => {
//         // item.href = navURLs[items.keys[item]];
//         window.location.href = navURLs[0];        
//     });
// });

console.log(items);

